<div class="container-fluid contenedor-custom">
    <div class="row justify-content-center custom-row">
        <form action="/editProfile/{{user.id}}" method="POST" class="formulario">
            <div>
                <input name="photoUrl" value="{{user.photoUrl}}" type="text" id="photoUrl" style="display: none;">
                <img style="width: 300px;" id="user-photo" src="{{user.photoUrl}}" alt="user-photo">
                <button type="button" id="upload_widget">Subir foto</button>
            </div>
            <div class="form-group">
                <label for="formGroupExampleInput">Nombre de usuario</label>
                <input name="username" type="text" class="form-control" id="formGroupExampleInput"
                    value="{{user.username}}" required>
            </div>
            <div class="form-group">
                <label for="formGroupExampleInput">Sobre mí</label>
                <input name="info" type="text" class="form-control" id="formGroupExampleInput" value="{{user.info}}"
                    required>
            </div>
            <div class="modal-footer">
                <button type="button" type="button" onclick="location.href='/profile';"
                    class="btn btn-danger">Cancelar</button>
                <button type="submit" class="btn btn-success">Aceptar</button>
            </div>
        </form>
    </div>
</div>
<script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>

<script src="/javascripts/cloudinarywidget.js"></script>